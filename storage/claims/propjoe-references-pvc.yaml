apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-propjoe-references
  namespace: jupyterhub
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-propjoe-hdd
  volumeName: nfs-propjoe-references
  resources:
    requests:
      storage: 900Ti
